# Данный скрипт обновляет текстовое значение здоровья игрока на экране
# Он прикрепляется к ноду Label, в который выводится значение здоровья

extends Label

func _physics_process(delta):
	text = str(GameManager.hp)
